= form_for @author, html: { class: "form form-horizontal author" } do |f|
  .form-group
    = f.label :given_name, class: 'control-label'
    .controls
      = f.text_field :given_name, class: 'form-control'
    = error_span(@author[:given_name])
  .form-group
    = f.label :family_name, class: 'control-label'
    .controls
      = f.text_field :family_name, class: 'form-control', required: true
    = error_span(@author[:family_name])
  = f.submit nil,class: 'btn btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), authors_path, :class => 'btn btn-default'
